import React from "react";
import { Route, Routes } from "react-router-dom";

const IndexRoutes = () => {
  return (
    <Routes>
      <Route></Route>
    </Routes>
  );
};

export default IndexRoutes;
